getwd() = Tells you where you are on computer
setwd() = Tells computers where you want to work in

setwd("~") = Tells computers to set wd to your home directory

eg.t1.dat <- read.csv("Task1.csv") = Tells R to read the data "Task1.csv" and put into eq.t1.dat

head(eg.t1.dat)

eg.t1.dat[10,]

eg.t1.dat[1:10,]